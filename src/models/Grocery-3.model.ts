export interface OfferCard {
  title: string;
  imgUrl: string;
  buttonText: string;
  discountOffer: string;
}

export interface MainCarouselItem {
  title?: string;
  imgUrl?: string;
  buttonLink?: string;
  buttonText?: string;
  description?: string;
}
